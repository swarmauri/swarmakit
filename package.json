{
  "name": "swarmakit",
  "version": "0.0.1",
  "private": false,
  "workspaces": [
    "libs/react",
    "libs/vue",
    "libs/sveltekit"
  ],
  "dependencies": {
    "@swarmakit/react": "file:libs/react",   // Reference the sub-packages via their file paths
    "@swarmakit/vue": "file:libs/vue",
    "@swarmakit/sveltekit": "file:libs/sveltekit"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "format": "pnpm -r run format"
  },
  "devDependencies": {
    "typescript": "^5.0.0",     
    "storybook": "^8.3.5"
  }
}
