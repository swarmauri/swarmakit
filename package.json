{
  "name": "swarmakit",
  "version": "0.0.1",
  "private": false,
  "workspaces": [
    "libs/react",
    "libs/vue",
    "libs/sveltekit"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "format": "pnpm -r run format"
  },
  "devDependencies": {
    "typescript": "^4.8.4",       
    "eslint": "^8.0.0",           
    "prettier": "^2.7.1",         
    "storybook": "^8.3.5"
  }
}